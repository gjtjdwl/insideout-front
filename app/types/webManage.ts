export interface departmentData {
  deptCode: string;
  departmentName: string;
  managerName: string;
}

export interface departmentUserData {
  userId: string;
  name: string;
  email: string;
  phoneNumber: string;
  role: string;
}
